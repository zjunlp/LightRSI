# Profile bundle patch: register the TokenPilot eviction plugin on the Host plane.
# Runs in agent/pre-step. Mutation remains off until the feature, estimator,
# eviction, and durable stateDir settings are all configured.
- insert:
    - id: tokenpilot-dsh
      name: '@lightrsi/deepseek-harness-adapter'
